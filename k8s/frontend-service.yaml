apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: devsecops
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 8080
  type: ClusterIP